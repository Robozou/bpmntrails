using bpmntrails;

namespace DCRReader
{
    //needs to visit each edge atleast once to see if traces expressed in bpmn is valid in dcr.
    internal static class Validator
    {
        static bool Validate(Processor graph, BPMNTrail trail)
        {

            return false;
        }
    }
}
